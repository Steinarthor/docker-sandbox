name: Docker sandbox

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  Test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Testing
        uses: actions/setup-node@v3
      - run: npm ci
      - run: npm test
